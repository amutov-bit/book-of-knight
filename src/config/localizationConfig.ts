/**
 * Translation asset location and locale fallbacks used during app boot.
 */
export const LOCALIZATION_CONFIG = {
  url: 'assets/localization/translations.json',
  defaultLocale: 'en',
  fallbackLocale: 'en'
} as const;
